# Generates multiplication table for a given number using while loop

n = int(input("Enter number whose table to show: "))
limit = int(input("Enter limit of table: "))

i = 1
while i <= limit:
    print(f"{n} x {i} = {n*i}")
    i += 1
